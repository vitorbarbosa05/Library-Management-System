import type {EditFormProps} from "@/src/(modules)/ui/table/row/row-action.tsx";
import type {AuthorResponse} from "@/src/(modules)/authors/types/author.types.ts";

export function FormUpdateAuthor({
                                     id,
                                     initialValues,
                                     onUpdated,
                                     onClose,
                                 }: EditFormProps<AuthorResponse>) {

}